package com.michaelflisar.composechangelog.gradle.plugin

import org.gradle.api.Plugin
import org.gradle.api.Project

class ClassLoaderPluginShared : Plugin<Project> {
    override fun apply(target: Project) {
        // no-op
    }
}